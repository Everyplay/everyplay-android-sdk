<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/splashscreen_bgColor" >

    <LinearLayout
        android:id="@+id/splashScreenLinearLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="false"
        android:layout_centerHorizontal="false"
        android:layout_centerInParent="true"
        android:orientation="vertical" >

        <ImageView
            android:id="@+id/splashScreenLogo"
            android:layout_width="wrap_content"
            android:layout_height="250dp"
            android:layout_marginTop="20dp"
            android:layout_marginLeft="30dp"
            android:layout_marginRight="30dp"
            android:layout_weight="0.3"
            android:scaleType="fitCenter"
            android:src="@drawable/everyplay_splashscreen_logo" />

        <ProgressBar
            android:id="@+id/splashScreenProgressBar"
            style="@android:style/Widget.DeviceDefault.ProgressBar.Large"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:layout_gravity="center"
            android:layout_marginTop="25dp"
            android:indeterminate="true"
            android:minHeight="35dp"
            android:minWidth="35dp" />

        <EditText
            android:id="@+id/splashScreenStatusText"
            style="android:none"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="20dp"
            android:background="@color/splashscreen_bgColor"
            android:clickable="false"
            android:ems="10"
            android:focusable="false"
            android:focusableInTouchMode="false"
            android:gravity="center"
            android:longClickable="false"
            android:textColor="@color/splashscreen_statusTextColor"
            android:textColorHint="@color/splashscreen_statusTextColor"
            android:textSize="18dp"
            android:text="@string/everyplay_loading_text" />

        <Button
            android:id="@+id/splashScreenRetryButton"
            android:layout_width="wrap_content"
            android:layout_height="45dp"
            android:layout_gravity="center"
            android:layout_marginBottom="20dp"
            android:layout_marginTop="20dp"
            android:background="@drawable/everyplay_button_green"
            android:minHeight="40dp"
            android:minWidth="100dip"
            android:textColor="@color/splashscreen_retryButtonTextColor"
            android:textColorHint="@color/splashscreen_retryButtonTextColor"
            android:text="@string/everyplay_retry_text" />
    </LinearLayout>

    <Button
        android:id="@+id/splashScreenCloseButton"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_margin="0dp"
        android:background="@drawable/everyplay_topbar_closebutton"
        android:minHeight="20dp"
        android:minWidth="20dp"
        android:width="20dp" />

</RelativeLayout>